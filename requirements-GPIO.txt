# GPIO control related requirements
# You need to install these with `sudo python3 -m pip install --upgrade --force-reinstall -q -r requirements-GPIO.txt`

# MPD client library for Python
python-mpd2>=3.1.0
